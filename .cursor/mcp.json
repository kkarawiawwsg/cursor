{
  "mcpServers": {
    "retell": {
      "command": "npx",
      "args": [
        "mcp-remote@latest",
        "https://mcp.retellai.com",
        "--header",
        "Authorization:Bearer ${env:RETELL_API_KEY}"
      ]
    }
  }
}
