# cursor

## MCP servers

This project configures the [Retell](https://www.retellai.com/) MCP server over HTTP transport in `.mcp.json`.

Set the `RETELL_API_KEY` environment variable before starting Claude Code so the server's `Authorization` header resolves:

```sh
export RETELL_API_KEY=<your-retell-api-key>
```
